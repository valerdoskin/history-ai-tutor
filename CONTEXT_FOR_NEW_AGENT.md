# CONTEXT_FOR_NEW_AGENT — Максимально подробная инструкция для нового разговора

**Дата создания:** 2026-08-30
**Дата последнего обновления:** 2026-09-02 (Этапы 1-9 плана PLAN_EXAM_UPGRADE.md завершены)
**Назначение:** Самодостаточный контекст для продолжения работы в НОВОМ разговоре.
Предыдущий разговор «подвисал» (превышал лимиты), поэтому пользователь попросил собрать
максимально подробную инструкцию, чтобы новый агент сразу понял:
1) что мы делаем (проект и его цель);
2) что уже сделано (полный аудит состояния);
3) что решили делать дальше (утверждённый план);
4) как работать (принципы, команды, защита от зацикливания).

> **ВАЖНО:** Этот документ — НЕ замена существующих md-файлов, а их сводка + актуальное
> состояние. Обязательно прочитай также:
> - `PLAN_EXAM_UPGRADE.md` — утверждённый план доработки под ОГЭ/ЕГЭ 2026 (**все 9 этапов выполнены**);
> - `RECOMMENDATIONS_EXAM_PREP.md` — исследование методик подготовки + структура экзаменов ФИПИ 2026;
> - `GLOBAL_PLAN.md` — общий план проекта (этапы 0-10);
> - `HANDOFF.md` — предыдущий handoff (УСТАРЕЛ по части данных, см. раздел 4 ниже);
> - `PLAN_VSEOB_10_FIX.md` — план доработки парсеров (в основном выполнен);
> - `PROMPTS.md`, `PROMPT_PLACEMENT_TEST.md`, `RAG_EVALUATION.md` — справочные.

---

## 1. Проект и его цель

**Что это:** ИИ-репетитор по истории для подготовки российских школьников к ОГЭ и ЕГЭ.

**Ключевой принцип (НЕ НАРУШАТЬ):** ИИ отвечает **только на основе локальной базы знаний**
(учебники, которые мы отпарсили). Никаких галлюцинаций и внешних источников.
LLM используется для генерации заданий/дистракторов/метаданных, но ответы ученику
всегда опираются на тексты учебников из `knowledge/chunks.json`.

**Архитектура (кратко):**
- Flask-сервер (`bot_webhook.py`) — API + Telegram Web App интерфейс.
- RAG-поиск по локальной базе знаний (Qdrant + эмбеддинги `intfloat/multilingual-e5-large`).
- LLM: DeepSeek (`deepseek-chat`) с fallback на Groq (`llama-3.1-8b-instant`).
- Банк вопросов ОГЭ/ЕГЭ (`knowledge/question_bank.json`) — основной источник заданий.
- Тест уровня (`placement_service.py`), адаптивная система (`adaptive_service.py`),
  практика ОГЭ/ЕГЭ (`exam_service.py`).

---

## 2. Окружение и как запускать

| Параметр | Значение |
|----------|----------|
| **Путь в контейнере** | `/workspace/history-ai-tutor` |
| **GitHub** | `valerdoskin/history-ai-tutor`, ветка `main` |
| **Python** | `/openhands/micromamba/envs/openhands/bin/python` (актуальный интерпретатор в этой сессии) |
| **Flask-порты** | `44167` (текущий, сервер запущен), `48235` (запасной) |
| **Qdrant** | Локальная БД `./knowledge/qdrant`, коллекция `history_tutor` |
| **Embedding модель** | `intfloat/multilingual-e5-large` |
| **LLM** | DeepSeek (`deepseek-chat`) с fallback на Groq |

### Ключевые настройки (в `.env`, секреты НЕ хранить в git)
- `DEEPSEEK_API_KEY`, `GROQ_API_KEY` — только в `.env` (файл в `.gitignore`).
- `DEEPSEEK_MODEL=deepseek-chat`, `GROQ_MODEL=llama-3.1-8b-instant`.
- `EMBEDDING_MODEL=intfloat/multilingual-e5-large`.
- `QDRANT_PATH=./knowledge/qdrant`, `QDRANT_COLLECTION=history_tutor`.
- `DB_PATH=./tutor_bot.db`.
- `KNOWLEDGE_DIR=./knowledge`, `CHUNKS_FILE`, `ENRICHED_FILE`.
- `RAG_TOP_K=5`, `RAG_SCORE_THRESHOLD=0.3`.
- `WEBAPP_HOST=0.0.0.0`, `WEBAPP_PORT=8080` (но локально запускается через `run_webapp_local.py`).

> **ВАЖНО:** DeepSeek/Groq API ключи хранятся только в `.env` (не в git). Не вставлять
> реальные ключи в коммиты — GitHub Push Protection блокирует push при обнаружении секрета.

### Запуск Flask-сервера (локально, без Telegram-бота)
```bash
cd /workspace/history-ai-tutor && python3 run_webapp_local.py
```
- Скрипт `run_webapp_local.py` запускает Flask без Telegram-бота, порт из `WEBAPP_PORT`
  (в текущей сессии фактически используется порт 44167).
- **Сервер уже запущен** (PID 5449), `/api/health` отвечает `{"status":"ok"}`.
- Проверка: `curl http://localhost:44167/api/health`

### Проверка RAG-поиска
```bash
curl -X POST http://localhost:44167/api/ask \
  -H "Content-Type: application/json" \
  -d '{"query":"Образование государства Русь","user_id":"null"}'
```

### Пересборка базы знаний (chunks.json)
```bash
cd /workspace/history-ai-tutor && python3 rebuild_knowledge.py
# Только для конкретной книги:
python3 rebuild_knowledge.py --only Vseobschaya_10
```

### Пересборка Qdrant
```bash
cd /workspace/history-ai-tutor && python3 rebuild_qdrant.py --chunks knowledge/chunks.json
```
> **ВАЖНО:** перед пересборкой Qdrant остановить Flask (Qdrant не поддерживает конкурентный доступ).

---

## 3. Текущее состояние (проверено 2026-09-02)

### Git
- **Ветка:** `main`, последний коммит `8a29eec` (Этап 8: Связать metadata чанков с кодификатором ФИПИ 2026).
- **Git status:** чистое рабочее дерево (нет незакоммиченных изменений).
- История коммитов (Этапы 1-8): `8a29eec` (Этап 8) -> `c949da4` (Этап 7) -> `b11d2f3` (Этап 6)
  -> `f2b21e1` (Этап 5) -> `653893f` (Этап 4) -> `d4c6e19` (Этап 3) -> `182a810` (Этап 2)
  -> `079d5cc` (Этап 1).
- Папка `knowledge/fipi_official/` в `.gitignore` (PDF ФИПИ не коммитим).
- `tutor_bot.db` в `.gitignore` (`*.db`), не отслеживается git.

### Flask-сервер
- **Запущен:** PID 5449 (`python3 -c "from bot_webhook import app; app.run(...)"`), порт 44167.
- `/api/health` отвечает `{"status":"ok"}`.
- Все API-роуты отвечают HTTP 200 (проверено через curl).

### База знаний (chunks.json)
- **Всего чанков: 6667** (список словарей).
- Структура чанка: `id, book_id, book_line, source_file, chapter_title, chapter_number,
  paragraph_title, paragraph_number, page_start, page_end, main_question, chunk_index,
  chunk_total, text, metadata`.
- `metadata` содержит: `dates, figures, terms, causal_links, key_facts, exam_questions, fipi_codes`.
- **Все 6667 чанков** имеют валидные `metadata.fipi_codes` в НОВОМ двухуровневом формате
  (секция.тема, например `"1.1"`, `"2.3"`) — приведено в соответствие с кодификатором 2026
  в Этапе 8 (коммит `8a29eec`).

### Обогащённые параграфы (enriched.json)
- **dict из 358 ключей** (по одному на параграф).
- Структура значения: `book_id, book_line, source_file, chapter_title, chapter_number,
  paragraph_title, paragraph_number, page_start, page_end, main_question, metadata`.

### Банк вопросов (question_bank.json)
- **dict из 1295 вопросов.** Формат: `{вопрос: {class, answer, type, distractors}}`.
- По типам: `fact` — 599, `cause_effect` — 348, `understanding` — 132, `term` — 94,
  `chronology` — 41, `comparison` — 31, `culture` — 30, `argumentation` — 20.
- По классам: 5 — 179, 6 — 157, 7 — 146, 8 — 181, 9 — 186, 10 — 161, 11 — 155.
- **Схема валидна:** 1295 записей, 0 нарушений схемы `{вопрос: {class, answer, type, distractors}}`.
- **ВАЖНО:** `llm_distractors.json` больше НЕ существует — он объединён в `question_bank.json`
  (коммит `696c729`). Дистракторы хранятся в поле `distractors` каждой записи.
- **Примечание:** во время браузерного тестирования Этапа 9 запущенный сервер автоматически
  добавлял дистракторы к вопросам chronology через `_save_question_to_bank`. Это изменение было
  ОТКАЧЕНО (`git checkout`), чтобы сохранить банк в закоммиченном состоянии. Не пугаться, если
  при тестировании банк снова изменится — это побочный эффект работающего сервера.

### Источники (source_questions.json)
- **dict из 28 источников.** Структура: `{source_text, source_title, paragraph, chapter,
  source_file, class, questions}`.
- Интегрированы в полноценный тест (Этап 5).

### Справочники по культуре и картам (НОВОЕ, Этапы 2-3)
- `knowledge/culture.json` — **список из 1125 записей** (не dict). Структура записи:
  `{name, type, author, year, period, class, fipi_codes, description, source_chunk_id}`.
  type: architecture | painting | sculpture | literature | music | science.
- `knowledge/maps.json` — **список из 188 записей** (не dict). Структура записи:
  `{name, period, class, description, key_objects, fipi_codes, source_chunk_id}`.

### Кодификатор ФИПИ (fipi_codifier.json)
- **Обновлён под структуру 2026** (Этап 1, коммит `079d5cc`).
- Формат: `{oge: {title, sections}, ege: {title, sections}}`, секция = `{code, title, topics: [{code, title}]}`.
- **ЕГЭ:** разделы 1-12 (история России до 1914 + всеобщая история, 1914-1945, ВОВ, СССР 1945-1991,
  РФ 1992-2022, всеобщая история 1914-2022).
- **ОГЭ:** разделы 1-6 (От Руси к Российскому государству, Россия в XVI-XVII вв., Россия в конце
  XVII-XVIII в., Российская империя в XIX - начале XX в., Всеобщая история, Новейшая история России).
  Для ОГЭ у topic есть поле `class`.

### Официальные PDF ФИПИ 2026 (knowledge/fipi_official/)
- `ИС-11 ЕГЭ 2026 КОДИФ.pdf` — кодификатор ЕГЭ 2026.
- `ИС-9 ОГЭ 2026_КОДИФ.pdf` — кодификатор ОГЭ 2026.
- Плюс СПЕЦ и ДЕМО для ЕГЭ и ОГЭ, `is_mr_2025.pdf` (методические рекомендации).
- Использованы как источники истины для Этапа 1 (структура кодификаторов 2026).

---

## 4. Что УЖЕ СДЕЛАНО (завершённые задачи)

### 4.1. Парсинг учебников (полностью)
- Отпарсили 13 учебников (5-11 классы) из DOCX/PDF в `knowledge/chunks.json` (6667 чанков).
- PDF-парсеры 11 класса с LLM-очисткой названий (`_clean_titles_with_llm`),
  нормализацией регистра (`_normalize_title_case`), склейкой переносов (`_join_hyphenated`).
- Исправлены парсеры: объединение номера и названия параграфов, разделение вспомогательных
  разделов, синхронизация ключей enriched.json и chunks.json (коммит `2377b23`).
- Нормализация названий глав и терминов (заглавные буквы, имена собственные) — коммит `b358bb0`.

### 4.2. Обогащение метаданных (LLM)
- `scripts/enrich_metadata.py` — обогащает параграфы датами, персоналиями, терминами,
  причинно-следственными связями, ключевыми фактами, экзаменационными вопросами, fipi_codes.
- Результат: `knowledge/enriched.json` (358 параграфов) и `metadata` в чанках.

### 4.3. Банк вопросов и дистракторы
- Создан единый `knowledge/question_bank.json` (1165 вопросов) — объединение
  `question_types.json` и `llm_distractors.json` (коммит `696c729`).
- LLM-дистракторы предвычислены и сохранены в банке (по 3+ на вопрос).
- **Принцип:** все сгенерированные LLM задания СОХРАНЯЮТСЯ в банк и ПЕРЕИСПОЛЬЗУЮТСЯ
  (функция `_save_question_to_bank` в `exam_service.py`). Не генерировать одно и то же заново.

### 4.4. RAG и Qdrant
- Qdrant пересобран (коллекция `history_tutor`), эмбеддинги вопросов построены
  (`question_embeddings.npy`, `answer_embeddings.npy`).

### 4.5. Формат ОГЭ/ЕГЭ и практика
- `exam_service.py`: генерация заданий по формату ФИПИ (типы fact, chronology,
  cause_effect, understanding, comparison, term), проверка ответов, полноценный тест.
- `placement_service.py`: тест уровня по выбранным классам, реестр вопросов
  (REGISTRY_SIZE=200 на класс), LLM-дистракторы с кэшем.
- Веб-интерфейс (`templates/webapp.html` + `static/webapp.js`): вкладки Чат, Практика,
  Темы, Карточки, Классы, Профиль. В «Практике» есть выбор ОГЭ/ЕГЭ и типа вопроса,
  кнопка «Полноценный тест (10 заданий)».
- Telegram-бот + Web App API (`bot_webhook.py`).

### 4.6. Доработка парсеров (PLAN_VSEOB_10_FIX.md)
- Исправлен парсер Vseobschaya_10 (фильтр ложных заголовков «МИР РОССИЯ»).
- Очистка мусора в книгах 7, 8, 9, 10 классов (буквы-обозначения карт, «ОГЛАВЛЕНИЕ»,
  служебные хвосты). В основном выполнено.

### 4.7. Исследование и план под ОГЭ/ЕГЭ 2026
- Скачаны официальные PDF ФИПИ 2026 в `knowledge/fipi_official/`.
- Создан `RECOMMENDATIONS_EXAM_PREP.md` (исследование методик + структура экзаменов).
- Создан `PLAN_EXAM_UPGRADE.md` (утверждённый план доработки, 9 этапов).
- Извлечена полная структура кодификаторов ЕГЭ и ОГЭ 2026 из PDF (см. раздел 6 ниже).

### 4.8. Этапы 1-9 плана PLAN_EXAM_UPGRADE.md (ВСЕ ЗАВЕРШЕНЫ, 2026-09-02)

**Этап 1 — Кодификатор ФИПИ 2026** (коммит `079d5cc`)
- `knowledge/fipi_codifier.json` обновлён под структуру кодификаторов ФИПИ 2026
  (ЕГЭ: 12 разделов, ОГЭ: 6 разделов с полем `class` у topic). JSON валиден, коды уникальны.

**Этап 2 — Справочник по культуре** (коммит `182a810`)
- Создан `knowledge/culture.json` — **1125 записей** (список, не dict). Данные строго из учебников.

**Этап 3 — Справочник по картам** (коммит `d4c6e19`)
- Создан `knowledge/maps.json` — **188 записей** (список, не dict).

**Этап 4 — Расширение банка вопросов** (коммит `653893f`)
- Банк расширен с 1165 до **1295 вопросов** (+130): хронология (+30), аргументация (+20),
  причинно-следственные (+30), сравнение (+20), культура (+30).
- Схема `{вопрос: {class, answer, type, distractors}}` сохранена.

**Этап 5 — Доработка exam_service.py** (коммит `f2b21e1`)
- В `TYPE_TO_FIPI` добавлены новые типы: `culture` {ege:[7,15,16], oge:[13,14]},
  `argumentation` {ege:[21], oge:[6]}, `map` {ege:[9,10,11,12], oge:[8,9,10]},
  `source` {ege:[6,13,14], oge:[17,18,19,20]}.
- Добавлены генераторы: `generate_culture_question()`, `generate_argumentation_question()`,
  `generate_map_question()`, `generate_source_question()`.
- `source_questions.json` интегрирован в полноценный тест.

**Этап 6 — Культура в тесте уровня и адаптивной системе** (коммит `b11d2f3`)
- `placement_service.py`: 2-3 вопроса по культуре в тесте уровня.
- `adaptive_service.py`: «культура» как отдельная тема/навык.
- Обновлены `placement_service.py`, `adaptive_service.py`, `bot_webhook.py`, `exam_service.py`.

**Этап 7 — Тренировочные режимы в веб-интерфейсе** (коммит `c949da4`)
- Backend `/api/train` + фронтенд. Режимы по типам: Культура, Карты, Источники, Аргументация,
  Хронология, Причинно-следственные, Сравнение. Каждый: выбор ОГЭ/ЕГЭ → серия → разбор ошибок.
- Проверено в браузере.

**Этап 8 — Связь metadata чанков с кодификатором** (коммит `8a29eec`)
- `metadata.fipi_codes` в chunks.json приведены к НОВОМУ двухуровневому формату (секция.тема).
- **Все 6667 чанков** имеют валидные fipi_codes. 30 тестов OK.

**Этап 9 — Тестирование и валидация** (завершён, без отдельного коммита — валидационный этап)
- Валидность JSON: culture.json (1125), maps.json (188), question_bank.json (1295),
  fipi_codifier.json, chunks.json (6667), enriched.json (358) — все OK.
- Схема question_bank.json: 1295 записей, 0 нарушений.
- Генерация всех типов (OGE и EGE из реестра + специализированные map/culture/argumentation/source) — OK.
- `generate_full_test` (10 заданий, 18 баллов) и `generate_placement_test` (тест уровня) — OK.
- `test_services.py`: **30 тестов OK** (использует `DB_PATH=/tmp/test_tutor_services.db`).
- RAG: `retrieve` и `grounded_answer` протестированы — ответы строго по контексту, без галлюцинаций.
- Веб-интерфейс (браузер): проверены ВСЕ вкладки — Чат (RAG), Практика (культура 5/5, полный тест),
  Темы (список тем, хронология, личности, термины 1436 с поиском), Карточки (SRS), Классы
  (7 классов + тест уровня), Профиль (уровень, XP, серия, точность, достижения).
- Все API-роуты отвечают HTTP 200.

---

## 5. Что НУЖНО СДЕЛАТЬ ДАЛЬШЕ (утверждённый план)

> **СТАТУС: ВСЕ 9 ЭТАПОВ ПЛАНА `PLAN_EXAM_UPGRADE.md` ЗАВЕРШЕНЫ (2026-09-02).**
> Ниже — справочная информация о плане (для истории). Подробности выполнения каждого этапа
> см. в разделе 4.8. Дальнейшие шаги (если пользователь захочет продолжить) — см. раздел 12
> «Открытые вопросы и возможные направления развития».

### Порядок выполнения (из PLAN_EXAM_UPGRADE.md) — ВЫПОЛНЕН
```
Этап 1 (кодификатор) --> Этап 8 (связь metadata с кодификатором)
        |
        v
Этап 2 (culture.json) --> Этап 4 (банк вопросов) --> Этап 5 (exam_service)
        |                        |
        v                        v
Этап 3 (maps.json) ----------> Этап 5 (exam_service)
                                      |
                                      v
                               Этап 6 (placement/adaptive)
                                      |
                                      v
                               Этап 7 (веб-интерфейс)
                                      |
                                      v
                               Этап 9 (тестирование)
```

### Краткое описание этапов (для справки, все выполнены)
- **Этап 1:** Обновить `knowledge/fipi_codifier.json` под структуру кодификаторов ФИПИ 2026
  (ЕГЭ: 12 разделов, ОГЭ: 6 разделов с полем `class`). ✅ (коммит `079d5cc`)
- **Этап 2:** Создать `knowledge/culture.json` (>=100 записей). ✅ (коммит `182a810`, 1125 записей)
- **Этап 3:** Создать `knowledge/maps.json` (>=40 записей). ✅ (коммит `d4c6e19`, 188 записей)
- **Этап 4:** Расширить банк вопросов (+130). ✅ (коммит `653893f`, 1165->1295)
- **Этап 5:** Доработать `exam_service.py` (новые типы culture/argumentation/map/source). ✅ (коммит `f2b21e1`)
- **Этап 6:** Интегрировать культуру в тест уровня и адаптивную систему. ✅ (коммит `b11d2f3`)
- **Этап 7:** Добавить тренировочные режимы в веб-интерфейс. ✅ (коммит `c949da4`)
- **Этап 8:** Связать metadata чанков с кодификатором. ✅ (коммит `8a29eec`)
- **Этап 9:** Тестирование и валидация. ✅ (завершён, без отдельного коммита)

---

## 6. Извлечённая структура кодификаторов 2026 (для Этапа 1)

### ЕГЭ 2026 (структура заданий, 21 задание, 42 первичных балла, 210 мин)
- Часть 1 (12 заданий с кратким ответом): 1 даты, 2 хронология, 3 факты, 4 систематизация,
  5 деятели, 6 источник, 7 **культура**, 8 изображения (ВОВ), 9-12 **карта**.
- Часть 2 (9 заданий с развёрнутым ответом): 13-14 источник, 15-16 изображения,
  17 ВОВ, 18 **причинно-следственные**, 19 понятия/термины, 20 **сравнение**, 21 **аргументация**.

### ОГЭ 2026 (структура заданий, 24 задания, 37 первичных баллов, 180 мин)
- Часть 1 (17 заданий): 1 даты/деятели, 2 хронология, 3 термин, 4 факты, 5 термин,
  6 тезисы/аргументация, 7 статистика, 8-10 **карта**, 11 изображение, 12 схема,
  13-14 **памятники культуры**, 15-16 зарубежная история, 17 источник.
- Часть 2 (7 заданий): 18-20 источник, 21 причины/следствия, 22 поиск ошибок,
  23 сравнение, 24 анализ ситуации.

### Ключевые выводы ФИПИ о типичных ошибках (ЕГЭ 2025, И.А. Артасов)
Самые сложные для выпускников: культура (7), личности (5), источник (6), карта (12),
атрибуция источника (13), понятия/термины (19), причинно-следственные (18),
сравнение (20), аргументация (21). Это подтверждает приоритеты доработки.

---

## 7. Ключевые файлы и их назначение

| Файл | Назначение |
|------|-----------|
| `bot_webhook.py` | Flask-сервер, все API-роуты (включая /api/train) |
| `config.py` | Конфигурация (читает .env) |
| `run_webapp_local.py` | Запуск Flask без Telegram-бота |
| `services/exam_service.py` | Практика ОГЭ/ЕГЭ, генерация заданий, TYPE_TO_FIPI (все типы) |
| `services/placement_service.py` | Тест уровня, реестр вопросов, дистракторы |
| `services/adaptive_service.py` | Адаптивная система, SRS-карточки |
| `services/rag_service.py` | RAG-поиск по базе знаний |
| `services/llm_service.py` | `call_llm()` — DeepSeek с fallback на Groq |
| `services/knowledge_service.py` | Работа с базой знаний |
| `templates/webapp.html` | Веб-интерфейс (вкладки) |
| `static/webapp.js` | Логика веб-интерфейса |
| `knowledge/chunks.json` | База знаний (6667 чанков, все с валидными fipi_codes) |
| `knowledge/enriched.json` | Обогащённые параграфы (358) |
| `knowledge/question_bank.json` | Банк вопросов (1295) |
| `knowledge/culture.json` | Справочник по культуре (1125 записей, список) |
| `knowledge/maps.json` | Справочник по картам (188 записей, список) |
| `knowledge/source_questions.json` | Источники (28) |
| `knowledge/fipi_codifier.json` | Кодификатор ФИПИ 2026 (обновлён в Этапе 1) |
| `knowledge/fipi_official/` | Официальные PDF ФИПИ 2026 (в .gitignore) |
| `scripts/enrich_metadata.py` | Обогащение метаданных (LLM) |
| `scripts/build_source_questions.py` | Построение source_questions.json |
| `scripts/classify_questions.py` | Классификация вопросов по типам |
| `scripts/precompute_llm_distractors.py` | Предвычисление дистракторов |
| `rebuild_knowledge.py` | Пересборка chunks.json |
| `rebuild_qdrant.py` | Пересборка Qdrant |
| `parsers.py`, `base_docx_parser.py`, `pdf_parsers.py` | Парсеры учебников |
| `PLAN_EXAM_UPGRADE.md` | **Утверждённый план доработки (главный для текущей работы)** |
| `RECOMMENDATIONS_EXAM_PREP.md` | Исследование методик + структура экзаменов |
| `GLOBAL_PLAN.md` | Общий план проекта |
| `HANDOFF.md` | Предыдущий handoff (УСТАРЕЛ по данным, см. ниже) |
| `PLAN_VSEOB_10_FIX.md` | План доработки парсеров (в основном выполнен) |

---

## 8. API-роуты (bot_webhook.py)

- `GET /` — веб-интерфейс
- `GET /static/<path:path>` — статика
- `POST /webhook` — Telegram webhook
- `POST /api/ask` — вопрос к репетитору (RAG)
- `GET /api/profile` — профиль
- `GET /api/exam` — получить задание практики (`?type=oge|ege&qtype=...`)
- `GET /api/exam/types` — список типов заданий (все типы, включая новые)
- `GET /api/train` — тренировочный режим по типу (`?type=oge|ege&qtype=...`)
- `GET /api/topics`, `GET /api/topic/<id>` — темы
- `GET /api/chronology`, `/api/figures`, `/api/terms` — справочники
- `GET /api/classes`, `GET/POST /api/user/classes` — классы
- `GET /api/placement`, `POST /api/placement/submit` — тест уровня
- `POST /api/exam/submit` — проверка ответа практики
- `GET /api/test`, `POST /api/test/submit` — полноценный тест
- `GET /api/progress` — прогресс
- `GET/POST /api/cards`, `POST /api/cards/review` — карточки
- `GET /api/health` — проверка здоровья

---

## 9. Принципы работы (защита от зацикливания)

1. **Каждый шаг — с чётким критерием завершения.** Если критерий не достигнут после 1-2
   попыток — фиксируем результат и переходим к следующей гипотезе, не крутимся.
2. **LLM — только с fallback.** При ошибке/некорректном ответе возвращаем исходные данные.
3. **Не перепарсиваем всё заново.** Только проблемные книги (через `--only`).
4. **Не трогаем работающий код.** Изменения точечные, в конкретных файлах.
5. **Проверка — разовая, по чек-листу**, не чаще, чем просит пользователь.
6. **Согласовывать каждое действие с пользователем**, не зацикливаться на диагностике.
7. **Все сгенерированные LLM задания сохраняются и переиспользуются** (в question_bank.json),
   не генерировать одно и то же заново.
8. **НЕ выдумывать данные** — всё должно опираться на тексты учебников в chunks.json.
9. **НЕ перечитывать PDF/chunks.json повторно** — извлекать данные за один проход скриптом.

---

## 10. Рекомендуемый порядок действий в НОВОМ разговоре

1. **Прочитать** `PLAN_EXAM_UPGRADE.md`, `RECOMMENDATIONS_EXAM_PREP.md` и этот документ.
2. **Проверить состояние:** `git status`, сервер (`/api/health`).
3. **Все 9 этапов плана завершены.** Если пользователь даст новую задачу — уточнить, что именно
   делать дальше (см. раздел 12 «Возможные направления развития»).
4. **Обновлять статус задач** по мере выполнения (см. раздел 11).

---

## 11. Статус задач (task_tracker)

| # | Задача | Статус |
|---|--------|--------|
| 1 | Этап 1: Обновить кодификатор ФИПИ под структуру 2026 | **done** (коммит `079d5cc`) |
| 2 | Этап 2: Создать справочник по культуре (culture.json) | **done** (коммит `182a810`, 1125 записей) |
| 3 | Этап 3: Создать справочник по картам (maps.json) | **done** (коммит `d4c6e19`, 188 записей) |
| 4 | Этап 4: Расширить банк вопросов (+130) | **done** (коммит `653893f`, 1165->1295) |
| 5 | Этап 5: Доработать exam_service.py (новые типы) | **done** (коммит `f2b21e1`) |
| 6 | Этап 6: Интегрировать культуру в тест уровня и адаптивную систему | **done** (коммит `b11d2f3`) |
| 7 | Этап 7: Добавить тренировочные режимы в веб-интерфейс | **done** (коммит `c949da4`) |
| 8 | Этап 8: Связать metadata чанков с кодификатором | **done** (коммит `8a29eec`) |
| 9 | Этап 9: Тестирование и валидация | **done** (завершён, без отдельного коммита) |
| 10 | Создать подробную инструкцию для нового разговора (этот документ) | **done** |

---

## 12. Открытые вопросы и возможные направления развития

### Решённые вопросы (из прошлых этапов)
- [x] Структура кодификатора 2026 подтверждена и записана в `fipi_codifier.json` (Этап 1).
- [x] Сопоставление старых трёхуровневых fipi_codes с новым двухуровневым кодификатором
      выполнено в Этапе 8 (все 6667 чанков приведены к новому формату).
- [x] Qdrant пересобран после изменений данных (chunks.json) в Этапе 8.

### Возможные направления развития (уточнить у пользователя)
- [ ] Пройти тест уровня в браузере до конца (все 24 вопроса) — функциональность подтверждена
      через API и юнит-тесты, но полный проход в UI не завершён.
- [ ] Расширить банк вопросов дальше (сейчас 1295) — например, добавить больше вопросов по
      культуре и картам (сейчас culture=30, map в реестре пуст для OGE/EGE).
- [ ] Улучшить качество автогенерируемых дистракторов для chronology-вопросов (при тестировании
      сервер добавлял не всегда корректные дистракторы — см. раздел 3).
- [ ] Интеграция с Telegram-ботом (проверка работы новых режимов через Telegram Web App).
- [ ] Дальнейшая оптимизация RAG (порог релевантности, качество ответов).
